name: publish
'on':
  - push

jobs:
  build-C-servers:
    permissions: write-all
    name: Build C Servers
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Install Tools
        run: |
          sudo apt upgrade
          sudo apt install build-essential cmake gbd -y